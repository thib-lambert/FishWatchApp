//
//  Color+Utils.swift
//  FishWatchApp
//
//  Created by Thibaud Lambert on 27/10/2020.
//

import SwiftUI

extension Color {

    struct SpeciesView {
        static let gradientBottom = Color(UIColor(netHex: 0x008A99))
        static let gradientTop = Color(UIColor(netHex: 0x5CCAD6))
        static let quote = Color(UIColor(netHex: 0x51627B))
        static let information = Color(UIColor(netHex: 0x3B8796))
    }

    struct App {
        static let background = Color(UIColor(netHex: 0xF0F0F3))
        static let scienceCell = Color(UIColor(netHex: 0x0093D0))
    }
}
